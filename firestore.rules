rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Locked down — Admin SDK bypasses rules
    // Frontend auth rules will be added in Step 2
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
